// TODO: Investigar cómo hacer estas pruebas unitarias

describe("GifExpertApp", () => {
  it("should add a new category", () => {

    // Preparar el arreglo de categorias
    // Disparar onAddCategory(newCategory)
    // Assert en categories para comprobar que se agregó la categoría
    // Assert para saber que se llamó setCategories

    expect(true);
  });

  it("should not add a new category if already exists", () => {

    // Preparar el arreglo de categorias
    // Disparar onAddCategory(newCategory) con una categoría existente
    // Assert para saber que no se llamó setCategories

    expect(true);
  });

  it("should delete category", () => {

    // Preparar el arreglo de categorias
    // Disparar onDeleteCategory(category)
    // Assert en categories para comprobar que se eliminó la categoría
    // Asset para saber que se llamó setCategories
    expect(true);
  });
});
